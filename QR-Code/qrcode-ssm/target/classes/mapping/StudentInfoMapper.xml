<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zhen.dao.StudentInfoDao">

    <resultMap id="StudentInfo" type="com.zhen.model.StudentInfo">
        <id column="student_id" property="studentId" jdbcType="VARCHAR"/>
        <result column="student_name" property="studentName" jdbcType="VARCHAR"/>
        <result column="student_sex" property="studentSex" jdbcType="VARCHAR"/>
        <result column="student_age" property="studentAge" jdbcType="BIGINT"/>
        <result column="student_home" property="studentHome" jdbcType="VARCHAR"/>
        <result column="create_time" property="createTime" jdbcType="DATE"/>
        <result column="update_time" property="updateTime" jdbcType="DATE"/>
        <result column="deleted" property="deleted" jdbcType="BIGINT"/>
    </resultMap>

    <select id="selectAllStudentInfo"  resultMap="StudentInfo">
        SELECT * FROM student_info WHERE deleted=0
    </select>

    <select id="selectStudentInfoById" parameterType="java.lang.String" resultMap="StudentInfo">
        SELECT * FROM student_info WHERE deleted=0 AND student_id=#{studentId}
    </select>

    <update id="updateStudentInfo" parameterType="com.zhen.model.StudentInfo">
        UPDATE student_info
         <set>
             <if test="studentName != null">
                 student_name = #{studentName},
             </if>
             <if test="studentSex != null">
                 student_sex = #{studentSex},
             </if>
             <if test="studentAge != null">
                 student_age = #{studentAge},
             </if>
             <if test="studentHome != null">
                 student_home = #{studentHome},
             </if>
             <if test="updateTime != null">
                 update_time = #{updateTime}
             </if>
         </set>
          WHERE student_id=#{studentId}
    </update>

    <delete id="deleteStudentInfo" parameterType="java.lang.String">
        DELETE FROM  student_info WHERE student_id=#{studentId}
    </delete>


    <insert id="addStudentInfo" parameterType="com.zhen.model.StudentInfo">
      INSERT INTO student_info(student_id,student_name,student_sex,student_age,student_home,create_time,update_time)
      VALUES (#{studentId},#{studentName},#{studentSex},#{studentAge}, #{studentHome},#{createTime},#{updateTime})
    </insert>


</mapper>