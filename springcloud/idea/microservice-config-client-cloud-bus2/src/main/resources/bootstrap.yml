spring:
  application:
    #对应config server所获取的配置文件的{application}
    name: cloudService
  cloud:
    config:
      uri: http://localhost:8080/
      profile: dev
      label: master
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest